
public struct JackScript {
    public private(set) var text = "JackScript!"

    public init() {
    }
}


/// Work-in-Progress marker
@available(*, deprecated, message: "work in progress")
@inlinable internal func wip<T>(_ value: T) -> T { value }

